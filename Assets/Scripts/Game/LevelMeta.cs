public struct LevelMeta
{
    public int ID;
    public string Name;
    public float Interval;

    public LevelMeta(int id, string name, float interval)
    {
        ID = id;
        Name = name;
        Interval = interval;
    }
}